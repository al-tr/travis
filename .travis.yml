language: java
jdk: oraclejdk8
install: true
script:
- ./gradlew check
- $([[ $TRAVIS_TAG ]] && echo "./gradlew publish") & true
- ./gradlew deploy
deploy:
- provider: script
  script: "./gradlew deploy"
  on:
    branch: deploy