language: java
jdk: oraclejdk8
cache:
  directories:
  - "$HOME/.gradle/caches/"
  - "$HOME/.gradle/wrapper/"
install: true
script:
- ./gradlew check
deploy:
- provider: script
  skip_cleanup: true
  script: 
  - ./gradlew publish & true
  - ./gradlew deploy
  on:
    branch: master